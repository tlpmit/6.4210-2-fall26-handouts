"""Code shared between psets, rather than belonging to any one of them.

Each module's own docstring says what it is for.  Scripts live one directory
down (ps1/, ps2/, ...) and are run from there, so each of them puts this
package's parent on `sys.path` before importing from it.
"""
